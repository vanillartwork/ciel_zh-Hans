# 更新记录

本文件记录**补丁**的版本。适用的**游戏**版本见
[`data/game-versions.csv`](data/game-versions.csv)。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

### 修正（安装与构建）

- **卸载无法还原字库。** 安装成功后安装程序会删掉 `$INSTDIRuild`，而卸载时
  `install.py --restore` 正是从那里读 `inplace.json`。计划没了，`do_restore()`
  里遍历原地写入步骤的循环一次都不执行，`Backup/` 里那 16.8 MB 区间备份成了
  **孤儿**——字节还在，但没有 `offset`／`size` 就写不回去。结果是卸载后
  `PACK00_01.PAK` 保持补丁状态，只能靠 Steam 校验文件重新下载 1.8 GB。
  现在安装成功时把还原计划写入 `Backup/inplace.json`，还原与校验优先读它。
  新增 2 项自检覆盖「删掉 build 之后仍能卸载」。
- **在已安装补丁的机器上构建，会产出更差的补丁。** `build_patch.py` 用
  `Pak(RES + "/PACK01.PAK")` 读游戏目录里的**实时**封包，而不是 `Backup/` 里的
  原件。补丁装好之后再构建，已经是中文的成员看起来「无变化」而被排除出暂存目录，
  打包器于是保留原始日文成员——上一版的译文被悄悄回退。实测有 125 个成员受影响，
  说话人名牌退回日文（`系统文本` → `システム文`）。改用 `gamepath.source_pak()`，
  与项目里其他需要原文的工具一致。产出文件数 2,605 → 2,679。

### 改动（性能）

- **构建（也就是安装）耗时减半：89 秒 → 38 秒。** 回注结果原先写成 2,679 个散
  文件，打包器再逐个读回。问题不在文件数本身，而在**杀软的按访问扫描**：刚写出来
  的文件第一次被打开时会被逐个扫描。实测读回这批文件首次要 **51.0 秒**，第二次
  只要 0.21 秒——差 250 倍，这也是为什么它容易被漏掉（只要测的是已存在的文件，
  就看不到）。现在改成一个 **ZIP_STORED 不压缩中间包**（`build/stage/text.zip`，
  22.6 MB）：只被扫描一次，写 0.10 秒、读 0.10 秒。
  安装程序正是在玩家机器上做这件事，而 Windows Defender 默认开启，所以这个收益
  对玩家是普遍的，不是本机特例。
  产出**逐字节不变**：两种暂存方式构建出的 `PACK01.PAK` SHA-256 相同
  （`e90ccc300d9ea350dc2aee4d`，32,490,801 字节），`inplace.json` 也相同。
  字体、两个可执行文件、原地修改计划仍按原方式处理；`build.py --loose` 保留原来的
  散文件布局，供人工查看暂存内容。
  中间包只在完整写完后才 `os.replace` 发布；读取时 CRC 校验失败、成员名重复、
  散文件与中间包混用都会**中止构建**，不会被当成「这个成员没有替换」——
  这四条失败路径都有测试覆盖。
- **字库构建内存峰值降低 64%：1,242 MB → 446 MB**（完整构建峰值 465 MB）。
  BC1 编解码原先一次处理整张 8192×4096 图集。大头有两处：解码里
  `take_along_axis` 会把索引提升成 `intp`，那个数组就是 268 MB；编码里
  表达式中的 `3.0` 是 Python float，把 `t` 和 `rint(t)` 都推成 float64，
  各 268 MB。现在按 **32 个块行（128 像素行）** 分批处理，写进预分配的缓冲区。
  运算本身一个字没改——每个值都只由自己那个 4×4 块算出，跟它在哪一批无关——
  数据类型、浮点运算、舍入方式、块顺序全部保持原样。块是按块行行主序排列的，
  所以按块行分批天然保持格式要求的顺序。
  **产出逐字节不变**：图集 16,777,260 字节与字库阶段的可执行文件都与原实现
  SHA-256 相同；另在 8 种尺寸（含非整批倍数的 124/132 行）和全 0／全 255／
  单点／渐变等边界块上与原实现对拍一致。耗时无回归（4.6 秒对 4.5 秒）。
  安装程序是在玩家机器上本地构建字库的，这一项直接减少低内存机器的换页。
- **封包异或改用 NumPy 批量运算。** 原来是逐字节的 Python 循环，重新打包时要跑遍
  封包每一个字节。16 MiB 实测 1.38 秒 → 0.094 秒；`PACK01` 打包 3.8 秒 → 1 秒。
  小于 4 KiB 的成员仍走原循环（建数组的开销超过收益），没有 NumPy 时自动回退，
  所以提取工具在裸 Python 环境下照样能用。输出**逐字节相同**：同一份暂存目录用
  两种实现分别打包，SHA-256 完全一致。

### 修正（提取）

- **夏尔（宠物）名字的字节预算量错了，少了三分之二。** 名字住在 32 字节的字段里，
  但打包工具把短名字写在长名字上面时不清尾巴——`アーキンス` 被 `シャラ` 覆盖后，
  字段里留着半截 `ンス`。提取器按「字符串 + 后面连续的 NUL 字节」估算可用空间，
  遇到这半截残留就提前停住，于是 32 字节的字段被量成了 10 字节。
  `tools/export_text.py` 新增 `BIN_SCHEMA`，给这两个文件写明记录格式
  （`sharl_name_data.bin` 72 字节/条、名字在 `+0x00`；`sharl_npcrating_data.bin`
  536 字节/条、名字在 `+0x1DC`），1,618 行里的 1,603 行恢复到完整的 32 字节预算。
  `import_text.py` 同步使用该格式，写回时会把残留字节一并清掉。
- 据此更正 `docs/not-translated.md`：这批数据**不是**「拼接用的词根，上限 4～6 字节，
  译不了」，而是 1,611 个**完整的宠物名**，每个可用 31 字节 = 10 个汉字，**可以翻译**。
  而且 npcrating 里的 1,434 个名字无一例外都是「名字池里的名字 + 57 个后缀之一」，
  翻 294 个基名 + 57 个后缀即可覆盖全部。
- 同一个文件里还有 **16 条不是名字的档案词条**（`アルトリー/エンプネル/ユーマ` ×
  `パミル科/メイス科/ランデ科`，以及 `悪魔の羽`、`丸い尻尾` 等尾巴翅膀名）
  从来没被翻译过——它们只在这个文件里出现，没有被其他表的译文覆盖到。
- `repo_export.py` 只认开头的「不修改」，两条明确标注不译的可执行文件字符串
  被算进了「待译」。改成在备注任意位置匹配。

### 新增（译文）

- **夏尔（宠物）名字全部汉化，1,602 行。** 译名进度 97.86% → **99.62%**。
  处理原则是「名字翻的是字形，不是意思」：
  - 原文是汉字的 96 个，只转字形不意译（`摩周雫`→摩周雫、`明茜鐘雫`→明茜钟雫）
  - 平假名里本身是词、写法固定的 17 个用该词的写法（`ひまわり`→向日葵）
  - 读音可对应多个汉字的 13 个，取最常见写法并在工作表里标注了备选
  - 外来语按**原始外语来源**音译（`シャノワ` ← chat noir → 夏诺瓦、
    `ミミル` ← 北欧神话 Mimir → 米米尔、`パトリシア` ← Patricia → 帕特里夏）
  - 官方译名优先（`イオナ` ← 官方 イオナサル=依欧娜萨鲁 → 依欧娜）
  1,434 个 NPC 名字全部由「294 个基名 + 57 个后缀」合成，`々` 叠字符号按基名末字展开。
- **夏尔档案词条 16 条**（9 条种族「科」名 + 尾巴翅膀名）此前从未翻译，已补齐。
  它们只在 `sharl_npcrating_data.bin` 里出现，没有被其他表的译文覆盖到。

### 修正（译名）

- **`みさき` 原先译作「海角」**——把名字当普通词译了。它在 `sharl_initrating_data.bin`
  里紧跟在性格字段后面，那张表也是夏尔档案，所以两处都是名字。改作「岬」。
  同类问题还有 `コーラス`→和声、`プール`→泳池，改为音译。
- **`normalize_cn.py` 漏了 4 个日本新字体**：`穂 黒 喩 緖`。zhconv 不认它们
  （既非繁体也非简体），映射表里也没有，于是原样漏过。已补。正文译文未受影响。
- **`normalize_cn.py` 新增 `normalize_name()`**，并把日文汉字的处理整理成三层规则：
  1. **中文没有这个字**（国字 `雫 笹 畑 辻 狛`）——没有可转的目标，保留。
  2. **只是另一个字的异体／新字体写法**（`黒 穂 緖 喩 姫 壷 壜 彌 凜 嶽`）——
     转成中文规范写法。人名也照转：`彌紗`→弥纱、`御嶽`→御岳。
  3. **中文里有分量的字**——保留。两种情况：本身是独立的字，合并会丢信息
     （`毬` 是毬果不是球体，机械转换会把 `陽毬` 变成「阳球」）；
     或者字典算异体但源语言当成不同的名字——**`倖田` 和 `幸田` 是日本两个不同的
     真实姓氏**，把 `倖` 并进 `幸` 不是简化写法，是把两个名字合成一个。
     `瑠` 同理，中文作品遇到它一向保留。
  第三层不是只看字典：字典关系说明「两个字形是不是同一个字」，
  不说明源语言把不把它们当同一个名字。
- 新增自查：列出所有译文里 GB2312 之外的汉字，逐个对照三层规则。
  这个自查查出正文里遗留的 `壷井彰久`（小提琴手姓名，`壷` 是 `壶` 的日本异体字）。

### 修正（术语）

- **「ジェノメトリクス」被错译成「梦世界」，与「夢セカイ」撞译名。**
  官方公开资料里的「夢世界」对应的是 `夢セカイ`，术语表却把它挂在了
  `ジェノメトリクス` 名下，于是两个不同的设定词在 119 行译文里合并成了同一个词。
  `ジェノメトリクス` 官方没有给出中文，现音译为**杰诺梅特利库斯**；
  `夢セカイ` 恢复为**梦世界**。另有两处一次性译法（「心之门」「杰诺矩阵」）一并统一。
  有一行原文同时含两个词，按出现顺序逐个对应处理。
- **「ダイブ」改译「潜行」，与续作官方译名一致。** 本作官方未公布该词的中文，
  续作《Ars nova》官方作「潜行」，且本作已有的 `ダイバー`→「潜行者」也与之相合。
  仅改动原文确实含 `ダイブ`／`ダブゲート` 的行；另有 31 行的「潜入」对应的是
  `侵入`、`紛れ込む`、`潜り`，属正确译法，未作改动。
  「潜行」不能像「潜入」那样直接带宾语，`潜行依欧娜萨鲁的内心` 等 6 行补了「至」。
- **「ジェノミライパンデミック」同时存在三种译法。** 官方作「瘟疫」，
  另外两种（「杰诺米莱伊大流行」「杰诺未来大流行」）已统一。
- 术语表此前把**游戏标题**也当成词条发布，其中「静籁之空」是民间译名、
  词条自己的备注就写着「非官方」，却被标成了官方且不可更改。标题不再进入术语表。
- 术语表新增「译名来源」区分：官方公开资料、续作官方译名、本项目约定，
  三者不再混为一谈；`apply_official.py` 重复运行不再把同一条来源备注盖两遍。

### 修正

- **说话人名牌没有被翻译。** 对白已是中文，名字仍显示「イオン」等日文——
  注入只写了对白所在的第 12 列，名牌所在的第 11 列从未写回。
  这个问题不容易发现，因为「神官」「少年」这类名字中日同形，看上去像已翻译。
- 安装程序的授权协议页乱码：`NOTICE-INSTALLER.txt` 缺 BOM，
  NSIS 按系统代码页读取。

### 修正（安装程序）

- **安装时弹出 python.exe 控制台窗口**（每个阶段一次）。上一版为了让工具能一边
  运行一边汇报进度，把 `nsExec::Exec` 换成了 `Exec`，而 `Exec` 不隐藏子进程窗口。
  现在改用 `pythonw.exe`（随安装包分发），并在工具里处理了 `sys.stdout` 为
  `None` 的情况——否则第一次 `print()` 就会让整个进程结束。
- **安装明细里的中文仍是乱码**。进度标签由 Python 写入文件、NSIS 读出显示，
  而 NSIS 的 `FileRead` 按系统代码页解码，UTF-8 必然出错。进度文件改为写
  **UTF-16LE（无 BOM）**，NSIS 改用 `FileReadUTF16LE`。
- **设置程序的原文读成了中文**。`env_strings.do_export` 直接读游戏目录下的
  `CielnosurgeDX_Env.exe`，而它可能已被上一版补丁改过，于是 54 条全部
  "原文不匹配"。新增 `gamepath.source_file()`，与封包一样优先读 `Backup`。
  这和之前给封包修的是同一类问题，当时漏了这个可执行文件。
- 欢迎页文字过长、与后面的页面重复、显示不全——已精简，并改用 Steam build
  编号标识游戏版本，另外写明**补丁完全免费，付费即是被骗**。
- **进度条现在显示真实进度。** 诊断安装包实测确认：NSIS 在每条指令之后都会
  重写进度条位置，所以自己发 `PBM_SETPOS` 不起作用，改控件 range 还会让它被
  钳到满格。现在的做法是隐藏 NSIS 那条、在同一位置新建一条自己的；工具被
  **分离启动**并把百分比写进 `progress.txt`，安装程序轮询它。百分比是真的
  子进度——提取按脚本条数、注入按文件数、打包按封包条目数，各阶段再映射到
  总进度的一段区间（`--progress-range`）。
- 安装明细里的乱码与中英文重复。NSIS 用系统代码页解码子进程管道，UTF-8 必然
  变成乱码；工具本身又会打印英文步骤名。现在工具把完整输出写入
  `install.log`，安装界面只显示自己的中文提示，出错时给出日志路径。
- 默认游戏路径中 `\` 与 `/` 混用导致必须手动重选。Steam 的注册表值用的是正
  斜杠。现在优先读取 Steam 为本作写入的卸载项
  （`Uninstall\Steam App 1477480` 的 `InstallLocation`），拿到的就是正确路径，
  并统一转换斜杠；手动选择仍然保留。
- 安装程序不再询问语言，只用简体中文。
- **卸载程序还原不了原地写入的那 16.8 MB**：`revert_step` 写好了却从来没有被
  调用，安装失败时的回滚同样漏了这一步。新增 `tools/selftest_install.py`
  （10 项）专门验证写入、重复安装、还原与各种拒绝情形。
- 卸载程序除注册到"应用和功能"外，另在游戏目录的 `Backup` 文件夹放一份副本。

### 说明

- 已确认 `PACK00_02.PAK`、`PACK00_03.PAK`、`PACK02.PAK` **不含任何面向玩家的
  文本**：逐条扫描后只有一个开发者脚本 `listup_size.js` 含日文，其余是角色
  贴图、场景模型与特效数据。补丁只需改 `PACK01.PAK`（文本）与
  `PACK00_01.PAK`（字库）。

### 新增

- **设置程序（`CielnosurgeDX_Env.exe`）汉化。** 分辨率、语言、手柄与按键
  设置界面共 54 条文本，位于其 Win32 资源中。对话框字体一并从
  MS UI Gothic 换为 SimSun —— 前者缺 17 个简体字，否则会显示成方块。
- 安装程序图标更换。

### 改动

- **`PACK00_01.PAK` 不再重建。** 新字体图集与原图集字节数完全相同
  （16,777,260），因此改为原地覆盖那一段。已验证原地写入与完整重建
  **逐字节相同**。
  - 构建临时占用从约 1.9 GB 降到约 160 MB
  - 该封包的备份从 1.8 GB 降到 16.8 MB
  - 构建耗时从约 142 秒降到约 94 秒
  - 安装所需可用空间要求从 6 GB 降到 1 GB
- `data/charset.txt` 改由 `tools/export_charset.py` 从**打好补丁的 exe 里那张
  字形表**导出。此前它被字体构建的中间产物覆盖过，含控制字节与重复项，
  与实际可绘字符相差近 300 个。
- `build.py` 新增 `--phase`，可分阶段执行，供安装程序汇报进度。


## [0.9.0] - 2025-09-12

首个公开版本。

### 新增

- 主线全 13 章、支线、日常事件的简体中文翻译（77,486 行，100%）
- 界面文本全部汉化（8,238 行）
- 道具名称与说明全部汉化（1,152 行）
- 可执行文件内 318 条硬编码日文字符串汉化：确认对话框、
  网络提示、菜单项、依恩的状态描述等
- 重建字库：9,351 个字形，GB2312 全覆盖，
  中文部分统一用同一种字体绘制
- NSIS 安装程序：核对游戏版本、自动备份、失败自动回滚、
  支持完整卸载还原
- 自动校验（`tools/validate.py`）：控制码、字节上限、字库覆盖、
  术语一致性等 13 项检查，不需要游戏即可运行
- 校验器自检（`tools/selftest_validate.py`），19 项
- 完整的格式与逆向资料（`docs/reverse-engineering/`）

### 修正

- 修正 105 行译文中残留的日文：未替换的 `{}` 术语，
  以及被当作中文保留的日文促音「ッ」「っ」
- 统一约 380 处跨章节的术语与人名漂移
- 修正 exe 字符串扫描器的正则，此前会在 `※ ★ ® ＆` 和全角数字处
  把字符串截断，导致 71 条（多为确认对话框）无法定位

### 已知问题

见 [docs/known-issues.md](docs/known-issues.md)。

[未发布]: https://github.com/vanillartwork/ciel_zh-Hans/compare/v0.9.0...HEAD
[0.9.0]: https://github.com/vanillartwork/ciel_zh-Hans/releases/tag/v0.9.0
