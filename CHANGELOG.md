# 更新记录

本文件记录补丁版本；适用的游戏版本见 [data/game-versions.csv](data/game-versions.csv)。

## [未发布]

### 翻译与术语

- 完成 1,602 条夏尔名字及 16 条档案词条翻译，文本完成度由 97.86% 提升至 99.62%。
- 夏尔名字按基名与后缀统一处理：汉字名规范字形，外来语名按来源音译，优先采用已有官方译名；`々` 按前一字展开。
- 修正将名字按普通词义翻译的问题，包括 `みさき`、`コーラス`、`プール`。
- 区分 `ジェノメトリクス`（杰诺梅特利库斯）与 `夢セカイ`（梦世界），修正 119 行混用及零散异译。
- `ダイブ` 统一为「潜行」，并调整相关句法；保留原文对应「侵入」等其他含义的「潜入」。
- `ジェノミライパンデミック` 统一为「瘟疫」。
- 移除术语表中的游戏标题条目，纠正将民间译名标为官方译名的问题。
- 区分本作官方资料、续作官方译名与项目约定；修正 `apply_official.py` 重复追加来源备注。
- 补齐说话人名牌的写回，修正对白已汉化而角色名仍为日文的问题。
- 完成环境设置程序 `CielnosurgeDX_Env.exe` 的 54 条界面文本，并将对话框字体改为 SimSun，避免缺字。

### 文本提取与字形处理

- 为夏尔名字表增加明确的 `BIN_SCHEMA`：名字字段宽度为 32 字节，修正尾部残留内容导致的容量低估；写回时清除残留字节。
- 修正 `repo_export.py` 对「不修改」备注的识别范围，避免将保留项计为待译。
- `normalize_cn.py` 补充 `穂`、`黒`、`喩`、`緖` 的字形映射。
- 新增 `normalize_name()`：规范可对应的异体或新字体写法，保留无对应字的国字及会影响姓名区分的字形。
- 增加 GB2312 之外汉字的复核，发现并修正「壷井彰久」中的异体字。
- `data/charset.txt` 改由最终主程序字形表导出，修正与实际可显示字符不一致的问题。

### 构建与安装

- 修正清理构建目录后无法还原字体图集的问题：还原计划保存到 `Backup/inplace.json`，还原与校验优先读取该文件，并增加相应自检。
- 修正已安装补丁时再次构建可能遗漏译文的问题：`build_patch.py` 使用原始备份封包作为输入。
- 环境设置文本提取改用 `gamepath.source_file()`，优先读取原始备份，避免将已汉化内容作为原文。
- `PACK00_01.PAK` 改为原地更新 16,777,260 字节的字体图集；验证结果与完整重建一致。该封包的备份量由约 1.8 GB 降至 16.8 MB。
- 修正卸载与失败回滚未恢复字体修改区间的问题，新增 `selftest_install.py`。
- `build.py` 新增分阶段执行参数 `--phase`，供安装器汇报进度。
- 安装器使用 `pythonw.exe`，避免各阶段弹出控制台窗口；工具兼容无标准输出的运行环境。
- 修正授权页与进度文本乱码：安装说明保留 UTF-8 BOM，进度文件使用 UTF-16LE，安装界面通过对应接口读取。
- 工具输出统一保存到 `install.log`，安装界面显示中文步骤及错误日志路径。
- 进度条按提取、注入、打包等阶段的实际进度更新。
- 优先读取 Steam 为本作记录的安装路径，并统一路径分隔符；保留手动选择功能。
- 精简欢迎页，使用 Steam build 编号说明游戏版本，并注明补丁免费。
- 安装界面统一使用简体中文，更新安装图标。
- 卸载程序除注册到 Windows 应用列表外，另在游戏目录的 `Backup/` 保存副本。

### 性能

以下为本地测试结果，实际耗时取决于硬件及安全软件。

- 文本暂存改为不压缩的 `stage/text.zip`，减少小文件访问。测试中完整构建由约 89 秒缩短至 38 秒，产物与散文件方式逐字节一致；`--loose` 保留散文件模式。
- 中间包完成写入后才替换目标文件；CRC 错误、重复成员或与散文件混用时中止构建。
- BC1 编解码改为分批处理，字体构建峰值内存由约 1,242 MB 降至 446 MB，输出保持一致。
- 封包异或在可用时采用 NumPy 批量运算，小成员及无 NumPy 环境保留标准库实现；测试中 `PACK01.PAK` 打包由约 3.8 秒缩短至 1 秒。
- 字体封包原地修改使构建临时占用由约 1.9 GB 降至约 160 MB，并降低备份空间需求。

### 文档与范围

- 精简项目说明、贡献指南与技术文档，统一安装、备份、数据格式及翻译状态说明。
- 更新夏尔名字的字段容量与完成状态。
- 确认 `PACK00_02.PAK`、`PACK00_03.PAK`、`PACK02.PAK` 无需本地化处理；资源修改集中于 `PACK01.PAK` 和 `PACK00_01.PAK`。

## [0.9.0] - 2025-09-12

首个公开版本。

### 新增

- 全 13 章主线、支线及日常事件翻译，共 77,486 行。
- 界面文本 8,238 行、道具名称与说明 1,152 行。
- 主程序内 318 条确认对话框、网络提示、菜单及状态文本。
- 包含 9,351 个字形的重建字库，覆盖 GB2312，中文使用统一字体。
- NSIS 安装程序，支持版本检查、备份、失败回滚与卸载还原。
- 无需游戏即可运行的数据校验工具及 19 项校验器自检。
- 文件格式与逆向资料。

### 修正

- 修正 105 行残留日文，包括花括号术语及促音。
- 统一约 380 处跨章节术语与人名差异。
- 修正程序字符串扫描在特殊符号和全角字符处截断的问题，补齐 71 条可定位字符串。

### 已知问题

见 [docs/known-issues.md](docs/known-issues.md)。

[未发布]: https://github.com/vanillartwork/ciel_zh-Hans/compare/v0.9.0...HEAD
[0.9.0]: https://github.com/vanillartwork/ciel_zh-Hans/releases/tag/v0.9.0
