# 参与贡献

欢迎参与。本项目需要的帮助大致分四类，**各自的要求不一样**，
请对照你要做的那一类往下看：

| 我想…… | 看这一节 | 需要会编程吗 |
|---|---|---|
| 改一句翻译、报个错字 | [改一两行](#改一两行) | 不需要 |
| 系统地翻译或校对一批文本 | [翻译与校对](#翻译与校对) | 不需要 |
| 讨论某个术语该怎么译 | [术语](#术语) | 不需要 |
| 改工具、改安装程序 | [代码](#代码) | 需要 |

> 提交任何内容前，请先读 [NOTICE.md](NOTICE.md) 中的
> **贡献者授权**一节。提交即表示你同意其中的条款。

---

## 改一两行

最省事的办法是**开一个 Issue**，附上截图，说明哪里不对、你觉得该怎么改。
不用自己动手改仓库。

如果你想直接改：在 GitHub 网页上找到对应的 `data/zh-Hans/**.csv`，
点铅笔图标，只改 `zh` 那一列，提交 Pull Request。

**只改 `zh`、`status`、`note` 三列。** `jp` 是游戏原文，
其余是工具生成的；改了会让校验失败（这是故意的——见下文）。

---

## 翻译与校对

### 准备

你需要：Python 3.10 以上。**翻译本身不需要装游戏**——
日文原文就在仓库里，和译文并排放着。

```bash
git clone https://github.com/vanillartwork/ciel_zh-Hans
cd ciel_zh-Hans
```

### 翻译

用任何能编辑 CSV 的工具打开 `data/zh-Hans/` 下的文件，对着 `jp` 列填 `zh` 列。

如果你更想在 git 之外工作（大量排序、批量替换），可以：

```bash
py -3 tools/hydrate.py             # 复制一份到 work/，git 会忽略它
py -3 tools/hydrate.py --game      # 顺便核对你的游戏版本对不对得上
```

改完用 `py -3 tools/dehydrate.py` 收回 `data/`。

推荐 [LibreOffice Calc](https://www.libreoffice.org/)：打开时
**编码选 UTF-8、分隔符选逗号、"引号内的字段视为文本"打勾**。
Excel 对 UTF-8 CSV 的处理容易出问题，用它之前请先看
[docs/editing-csv.md](docs/editing-csv.md)。

动笔前请先读 [docs/style-guide.md](docs/style-guide.md)，
尤其是**不能改的技术内容**那一节。简单说：

- `<CLBU>` `<CLY1>` `<CR>` 这类尖括号标记 —— **原样保留**
- `{……}` 花括号 —— **括号保留，里面的词按术语表翻译**
- `%s` `%5d` `$0$` 这类占位符 —— **原样保留**
- 换行 —— 可以少，**不要多**

### 交回去

```bash
py -3 tools/validate.py       # 检查
git add data && git commit -m "翻译第 6 章前半"
```

**不要改 `jp` 列。** 它是游戏里的原文，`src` 和 `ctl` 都由它算出来，
校验时会核对——改了 `jp` 会直接报错。

用 `work/` 工作的话，`dehydrate.py` **只带回 `zh`、`status`、`note`**，
其余一律以 `data/` 为准，所以也不必担心误伤。

### 校验必须通过

`tools/validate.py` 报的**错误**会挡住合并，**警告**不会。
它不评价译得好不好——那是评审的事——它只管：

| 检查 | 为什么 |
|---|---|
| 控制码有没有丢 | 丢了颜色标记会串行 |
| 换行有没有变多 | 多一行可能超出文本框 |
| 字节数有没有超 | 字节受限字段写超了会截断甚至崩溃 |
| 有没有字不在字库里 | 会显示成方块 |
| 有没有残留假名 | 基本都是漏译 |
| 同一句原文有没有译成两样 | 菜单/道具必须统一 |
| 有没有用到术语表里的**禁止译法** | 名字必须前后一致 |

确实需要破例时，**不要去改检查**，而是在
[`data/allow.csv`](data/allow.csv) 里加一行，写清楚理由。
这一行会出现在 diff 里，和别的改动一样接受评审。

---

## 术语

术语表是 [`data/glossary/glossary.csv`](data/glossary/glossary.csv)。

- 想改一个已有译名 → 开 [术语讨论 Issue](../../issues/new?template=terminology.yml)，
  **不要直接改**。名字一旦改动会牵连几十上百行，要先讨论。
- 想补充一个新词 → 直接提 PR 加一行。

`authority` 列写的是这个译名的依据。
标着 `光荣特库摩台湾官方公开资料` 的，是官方已经公开使用过的译名，
**原则上不改**，即使有人觉得别的译法更好——统一比完美重要。

`forbidden` 列是**明令禁止的译法**，一般是本项目历史上真的出现过的错译。
CI 会拦住它们。

---

## 代码

改 `tools/`、`installer/` 的 PR，额外要求：

- **说清楚你怎么验证的。** 涉及封包、字形表、PE 结构的改动，
  请附上你跑过的命令和输出。
- 改到注入或打包逻辑，请跑一次
  `py -3 tools/test_roundtrip.py`，确认空改动重建出来的封包和原始封包**逐字节相同**。
- 风格跟着周围的代码走。注释写**为什么**，不写代码在干什么。
- 不要引入新的第三方依赖，除非确实必要。目前只有
  `numpy` 和 `Pillow`，而且只在构建字体时需要。

### 不接受的内容

- 与 DRM、授权验证、防盗版、访问控制相关的分析或绕过代码
- 机翻结果，或来路不明、抄自其他汉化组的译文
- 任何游戏原始文件：封包、可执行文件、美术、音频、完整日文剧本

最后一条 CI 会自动拦截，安装包的构建脚本也会再查一次。

---

## 评审怎么进行

- **译文 PR** 由至少一位熟悉这部作品的人过一遍，重点看术语一致和语感。
  不追求逐字精确，追求读起来像人话。
- **代码 PR** 重点看会不会把游戏改坏，以及出错时能不能恢复。
- 校验全绿不等于可以合并，但校验不绿一定不能合并。

耐心一点——这是业余项目，评审可能要几天。

---

## 行为准则

就事论事，对事不对人。翻译上的分歧很正常，
讨论时请说明理由，而不是只说"这样不对"。
